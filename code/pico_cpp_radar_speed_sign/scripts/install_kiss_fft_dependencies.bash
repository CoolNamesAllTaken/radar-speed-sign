# !/bin/bash
sudo apt install -y pkg-config # Fixes could not find PkgConfig issue with CMAKE.
sudo apt install -y libpng-dev

# changed stuff in CMakeLists.txt to turn a bunch of this stuff off, hopefully don't need this file anymore!